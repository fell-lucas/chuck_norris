name: chuck_norris
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter_spinkit: ^5.1.0
  get_it: ^7.2.0
  equatable: ^2.0.3
  flutter_bloc: ^7.1.0
  joke_repository:
    path: "packages/joke_repository"
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
